<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>
        <variable
            name="bind"
            type="com.example.githubproject.data.GithubMyWork"/>
    </data>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="60dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/img_rv_item_my_work"
        android:layout_width="60dp"
        android:layout_height="60dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:src="@drawable/noun_repository"
        android:layout_marginLeft="20dp"
        />
    <TextView
        android:id="@+id/txt_rv_item_my_work"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toEndOf="@+id/img_rv_item_my_work"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:lines="2"
        android:text="@{bind.name}"
        android:textSize="25dp"
        />

</androidx.constraintlayout.widget.ConstraintLayout>
</layout>